name: hp
channels:
  - conda-forge
dependencies:
  - python=3.9
  - pip>=24
  - setuptools>=77
  - wheel>=0.43
  - cairo>=1.18
  - pip:
      # Run `conda env create -f environment.yml` from the repository root.
      # Python dependencies and the editable package come from pyproject.toml.
      - "-e .[dev]"
